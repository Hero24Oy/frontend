import { NativeStackNavigationOptions } from '@react-navigation/native-stack';

import { Color } from '@hero24/ui';

export const stackConfig = {
  contentStyle: {
    backgroundColor: Color.WHITE_00,
  },
} satisfies NativeStackNavigationOptions;
