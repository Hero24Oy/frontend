{
  "compilerOptions": {
    "baseUrl": "./src",
    "paths": {
      "$providers": ["providers"],
      "$components": ["components"],
      "$components/*": ["components/*"],
      "$atoms": ["atoms"],
      "$theme": ["theme"],
      "$theme/*": ["theme/*"],
      "$types": ["types"],
      "$hooks": ["hooks"],
      "$icons/*": ["icons/*"]
    }
  },
  "extends": "@hero24/configs/tsconfig.json",
  "exclude": ["node_modules", "dist"],
  "include": ["**/*.ts", "**/*.tsx"]
}
