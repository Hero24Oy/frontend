export enum DescendantStyleName {
  TEXT = '_text',
  ICON = '_icon',
  SPINNER = '_spinner',
  INPUT = '_input',
  INDICATOR = '_indicator',
  LABEL = '_label',
  DIVIDER = '_divider',
}

export enum AncestorStyleName {
  DIVIDER = '_divider',
  TEXT = '_text',
  LABEL = '_label',
  BUTTON = '_button',
  SELECT = '_select',
}
