export enum DescendantStyleName {
  TEXT = '_text',
  ICON = '_icon',
  SPINNER = '_spinner',
}

export enum AncestorStyleName {
  BUTTON = '_button',
}

export enum Size {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
}
