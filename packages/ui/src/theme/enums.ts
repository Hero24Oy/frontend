export enum DescendantStyleName {
  TEXT = '_text',
  ICON = '_icon',
  SPINNER = '_spinner',
}

export enum AncestorStyleName {
  BUTTON = '_button',
}

export enum Size {
  SMALL = 'sm',
  MEDIUM = 'md',
  LARGE = 'lg',
}
