import { ViewProps } from 'react-native';

import { ComponentTheme } from '$theme/types';

export const CustomActionsheetContent = {
  theme: {
    alignItems: 'flex-start',
  },
} satisfies ComponentTheme<ViewProps>;
