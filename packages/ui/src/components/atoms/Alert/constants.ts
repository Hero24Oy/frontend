export enum AlertAction {
  ERROR = 'error',
  WARNING = 'warning',
  SUCCESS = 'success',
  INFO = 'INFO',
  MUTED = 'muted',
}

export enum AlertVariant {
  SOLID = 'solid',
  OUTLINE = 'outline',
  ACCENT = 'accent',
}
