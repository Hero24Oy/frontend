import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  cell: {
    width: 50,
    height: 60,
    lineHeight: 55,
    fontSize: 14,
    borderWidth: 1,
    borderRadius: 8,
    borderColor: '#DDDDE1',
    textAlign: 'center',
    textAlignVertical: 'center',
  },
  focusCell: {
    borderColor: '#000',
  },
});
