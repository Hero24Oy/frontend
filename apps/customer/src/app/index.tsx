import { FC } from 'react';

import { SafeAreaView, View } from '@hero24/ui';

const Home: FC = () => {
  return (
    <SafeAreaView>
      <View>Hello, world</View>
    </SafeAreaView>
  );
};

export default Home;
