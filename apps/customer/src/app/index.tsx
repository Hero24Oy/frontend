import { FC } from 'react';

import { SafeAreaView, Text } from '@hero24/ui';

const Home: FC = () => {
  return (
    <SafeAreaView>
      <Text>Hello, world</Text>
    </SafeAreaView>
  );
};

export default Home;
