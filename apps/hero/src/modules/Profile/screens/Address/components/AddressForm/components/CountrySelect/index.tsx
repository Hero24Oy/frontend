import { FC } from 'react';

import { Text } from '@hero24/ui';

export const CountrySelect: FC = () => {
  // TODO -- add country select component (Select UI component need)
  return <Text variant="medium">Country selector here</Text>;
};
