import { FC } from 'react';

import { ResetPasswordByEmailVerification as CommonResetPasswordByEmailVerification } from '@hero24/common';

export const ResetPasswordEmailVerificationScreen: FC = () => {
  return <CommonResetPasswordByEmailVerification />;
};
