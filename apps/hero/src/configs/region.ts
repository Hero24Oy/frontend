import { CountryCode } from 'react-native-country-picker-modal';

// temporary configs until we support only FI
export const supportedCountry: CountryCode = 'FI';

export const DEFAULT_POSTCODE_LENGTH = 5;

export const POSTCODE_REGEX = /^\d+$/;
