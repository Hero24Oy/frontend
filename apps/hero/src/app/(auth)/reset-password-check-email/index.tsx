import { FC } from 'react';

import { ResetPasswordCheckEmailScreen } from '@hero24/common';

const ResetPasswordCheckEmail: FC = () => {
  // const onSuccessResetPasswordHandler = (): void => {
  //   router.push('/');
  // };

  return <ResetPasswordCheckEmailScreen />;
};

export default ResetPasswordCheckEmail;
