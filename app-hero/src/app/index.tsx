import React, { FC } from 'react';
import { SafeAreaView, Text, View } from 'react-native';

const Home: FC = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Hello, world</Text>
      </View>
    </SafeAreaView>
  );
};

export default Home;
