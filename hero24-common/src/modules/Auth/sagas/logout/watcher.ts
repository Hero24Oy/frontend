// import { call, take } from 'redux-saga/effects';

// import { logoutWorker } from './workers';

// import { authSucceed, logout } from '$modules/Auth/actions';

// export function* logoutWatcher(): Generator {
//   while (true) {
//     yield take(authSucceed.getType());

//     yield take(logout.getType());
//     yield call(logoutWorker);
//   }
// }
